#HOW to config in local and RUN

* Checkout the repo in to local
* Install docker in the local machine 
    * Note : If Windows need to make kernal as container 
* Run 
    * `docker-compose build`
    * `docker-compose up`

